cl_showbackpackrarities "1" //the reason this one specific command needs to be placed directly inside config.cfg is because there is currently a bug in TF2 that prevents this command from automatically populating the config.cfg file when it is run from autoexec.cfg - the reason no one notices this is because cloud sync usually handles this after the user manually changes the value in-game for the first time
